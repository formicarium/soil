#merge [#include "base.edn"
        {:env           "dev"
         :config-server {:url "http://localhost:8081"}
         :kubernetes    {:proxy {:url "http://localhost:9000"}}
         :formicarium   {:domain "formicarium.host"}
         :skip-kubectl-proxy false}]
